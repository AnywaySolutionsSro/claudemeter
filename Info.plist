<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleName</key>
	<string>ClaudeMeter</string>
	<key>CFBundleDisplayName</key>
	<string>ClaudeMeter</string>
	<key>CFBundleIdentifier</key>
	<string>com.jakubzak.claudemeter</string>
	<key>CFBundleExecutable</key>
	<string>ClaudeMeter</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>CFBundleIconName</key>
	<string>AppIcon</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0.1</string>
	<key>CFBundleVersion</key>
	<string>2</string>
	<key>LSMinimumSystemVersion</key>
	<string>13.0</string>
	<!-- Menu-bar agent: no Dock icon, no main window.
	     NB: removing LSUIElement (relying only on the runtime .accessory policy) does
	     NOT make the macOS 26 widget gallery render the app's sidebar chip icon — it
	     stays blank either way — and it causes a Dock icon to appear at launch. So we
	     keep LSUIElement and accept the blank gallery chip (cosmetic; placed widgets
	     and their in-widget icons render fine). See CLAUDE.md gotchas. -->
	<key>LSUIElement</key>
	<true/>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<!-- REQUIRED for auto-resume: without this key macOS silently auto-DENIES
	     every Apple Event (no consent prompt is ever shown), and iTerm2 control
	     fails with "Not authorized to send Apple events". -->
	<key>NSAppleEventsUsageDescription</key>
	<string>ClaudeMeter types “continue” into your iTerm2 session to auto-resume Claude Code after your usage quota refills.</string>
	<key>NSHumanReadableCopyright</key>
	<string>ClaudeMeter — Claude subscription usage in the menu bar.</string>
	<key>NSSupportsSuddenTermination</key>
	<true/>
</dict>
</plist>
